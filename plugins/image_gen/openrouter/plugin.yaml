name: openrouter
version: 1.0.0
description: "OpenRouter + Eden Portal image generation (chat-completions image output; reference-grounded). Text-to-image and image-to-image."
author: Eden OE
kind: backend
requires_env:
  - OPENROUTER_API_KEY
